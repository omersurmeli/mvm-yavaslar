document.getElementById('year').textContent=new Date().getFullYear();const menu=document.querySelector('.menu'),nav=document.getElementById('nav');menu.addEventListener('click',()=>{const open=nav.classList.toggle('open');menu.setAttribute('aria-expanded',open)});nav.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{nav.classList.remove('open');menu.setAttribute('aria-expanded','false')}));const board=document.querySelector('.board'),slides=[...board.querySelectorAll('.slide')],dots=[...board.querySelectorAll('.dots button')],counter=board.querySelector('.counter');let current=0,timer;function show(i){current=(i+slides.length)%slides.length;slides.forEach((s,n)=>{s.classList.toggle('active',n===current);s.setAttribute('aria-hidden',n!==current)});dots.forEach((d,n)=>{d.classList.toggle('active',n===current);d.setAttribute('aria-selected',n===current)});counter.textContent=`${String(current+1).padStart(2,'0')} / ${String(slides.length).padStart(2,'0')}`}function play(){clearInterval(timer);if(!matchMedia('(prefers-reduced-motion:reduce)').matches)timer=setInterval(()=>show(current+1),5500)}board.querySelector('.prev').onclick=()=>{show(current-1);play()};board.querySelector('.next').onclick=()=>{show(current+1);play()};dots.forEach((d,i)=>d.onclick=()=>{show(i);play()});board.onmouseenter=()=>clearInterval(timer);board.onmouseleave=play;show(0);play();
